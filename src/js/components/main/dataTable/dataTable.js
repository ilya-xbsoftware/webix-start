import { SMALL_FILM_SET } from "@js/constants/testdata";

const dataPath = ;

const dataTable = {
  view: "datatable",
  id: "filmsTable",
  autoConfig: true,
  url:dataPath,
  scrollX: false,
};

export { dataTable };
