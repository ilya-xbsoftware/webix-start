import { SMALL_FILM_SET } from '@js/constants/testdata';

const dataTable = {
  view: 'datatable',
  id: "filmsTable",
  autoConfig: true,
  data: SMALL_FILM_SET,
  scrollX: false,
};

export { dataTable };
