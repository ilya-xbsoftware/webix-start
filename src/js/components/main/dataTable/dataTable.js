import { SMALL_FILM_SET } from '@js/constants/testdata';

const dataTable = {
  view: 'datatable',
  autoConfig: true,
  data: SMALL_FILM_SET,
  css: 'dataTable',
  scrollX: false,
};

export { dataTable };
